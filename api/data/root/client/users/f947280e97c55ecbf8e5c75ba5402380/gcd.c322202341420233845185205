#include <stdio.h>
int min(int a, int b) {
    if (a < b) {
        return a;
    } else {
        return b;
    }
}

int Consecutive(int m, int n) {
    int t = min(m, n);
 
    while (1) {
        if (m % t == 0) {
            if (n % t == 0) {
                return t;
            } else {
                t--;
            }
        } else {
            t--;
        }
    }
}


int main(){
    int box[3][2] = {{60,24}, {31415,14142}, {1155,12597}};

    for(int i=0;i<3;i++){
        int gcd = Consecutive(box[i][0], box[i][1]);
        printf("GCD: %d\n", gcd);
    }
    return 0;
}